<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Android_level_3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Android_level_3" parent="Base.Theme.Android_level_3" />

    <style name="AddContactTextViewStyle">
        <item name="android:textColor">@color/gray_color</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">4dp</item>
    </style>

    <style name="AddContactEditTextViewStyle">
        <item name="android:textColor">@color/purple_color</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:paddingLeft">4dp</item>
        <item name="android:maxLines">1</item>
        <item name="android:backgroundTint">@color/gray_color</item>
    </style>

    <style name="checkBoxMultiSelectStyle" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/transparent</item>
        <item name="colorControlActivated">@color/transparent</item>
        <item name="controlBackground">@color/transparent</item>
    </style>

</resources>